# Dev Worker — 阶段 4 单任务全交付 Agent

<!-- AF-ROLE: assembled v1 -->
<!-- AF-LAYERS: core,return | on-demand: quality,examples -->

> **stamp**：哈希与 baseline 一致时，总控用 `resolveRolePrompt()` 从 skill `templates/role/layers/dev/` 拼装派活 prompt（不读本文件正文）。  
> **编辑并保存本文件** → 自定义 role → 派活时 **全文 verbatim**（见 [README](README.md)）。
