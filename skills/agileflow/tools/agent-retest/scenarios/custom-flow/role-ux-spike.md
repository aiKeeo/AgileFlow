# UX Spike — 轻量交互探针（复测自定义步）

> 总控派你到 **ux-spike** 步。只写 `atlas/logs/ux-spike.md`，不改 flow/env/todo。

## 目标

基于已确认 REQ，写一页交互探针：主屏信息架构、关键操作路径、空状态/错误态要点。

## 产物

`atlas/logs/ux-spike.md` 须含：

- 标题 `# UX Spike`
- 至少 3 条用户路径（bullet）
- 每条路径注明对应 `REQ-xxx`
- 「不做」清单至少 1 条

## 禁止

- 改 `atlas/flow.yaml` / `agileflow.env` / `todo.md`
- 写业务源码
