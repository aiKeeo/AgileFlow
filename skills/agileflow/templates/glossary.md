# 术语表

> 自动维护：init refresh / REQ 新增时自动扫描新术语。  
> 标 `<!-- auto -->` 的条目由系统提取，可人工补充释义。  
> 手动条目无 `<!-- auto -->` 标记。

## 业务术语

| 术语 | 英文/缩写 | 释义 | 来源 | 标记 |
|------|-----------|------|------|------|
| {术语} | {EN} | {一句话释义} | REQ-xxx / init/p0-business | <!-- auto --> |

## 技术术语

| 术语 | 缩写 | 释义 | 来源 | 标记 |
|------|------|------|------|------|
| — | — | — | — | — |

## 内部暗语（如适用）

| 暗语 | 正式名 | 释义 |
|------|--------|------|
| — | — | — |

---

## 自动扫描规则

> 以下场景触发术语扫描：

| 场景 | 扫描来源 | 动作 |
|------|----------|------|
| init 落盘 / refresh | README、docs/、代码 Enum/常量注释、字段 comment | 提取候选术语 → 写入本表（标 `<!-- auto -->`） |
| REQ 新增 | REQ 标题、AC 表中的业务名词、model 实体名 | 同上 |
| model 新增/变更 | 聚合根名、实体名、值对象名 | 同上 |
| solution 新增 feature | feature 标题中的业务名词 | 同上 |

**权威路径**：全项目术语**只**维护本文件 `atlas/glossary.md`（阶段 1 / model / solution / init refresh 都写这里）。

| 场景 | 动作 |
|------|------|
| greenfield / 阶段 1 | 术语再少也写本文件；**禁止**建 `atlas/init/` |
| brownfield init | `p0-business.md` 可留 3~5 个总览词并**链到本文件**；完整表在本文件 |

**人工补充**：扫描只提取术语名和来源；释义须人工填写（或 AI 自主推断后标记）。
