---
target: dev/T-*.md
summaryBullets: 本T,做,不做,上游,AC
---

# [T-001] 任务 — 构思

> **标准/完整**：用原子步骤表（每 `####` 一个步骤 + 8 字段规格表）。
> 8 字段必须全列（值可填"无"，行不能省）：执行角色 / 触发条件 / 输入数据 / 处理逻辑（含所有 if/else）/ 调用依赖（`Service.method(params)`）/ 异常处理（错误码+回滚）/ 输出数据 / 状态变更
> **精简**：可用 `####` + **涉及改动**（`Class.method()` — 边界说明）。
> 例子 → exemplar-BE / exemplar-FE

## 摘要

- **本 T**：…
- **做**：…
- **不做**：…
- **上游**：…
- **AC**：…

## 步骤

#### S1：步骤名称

| 字段 | 内容 |
|------|------|
| 执行角色 | … |
| 触发条件 | … |
| 输入数据 | … |
| 处理逻辑 | ① … |
| 调用依赖 | `Class.method()` |
| 异常处理 | … |
| 输出数据 | … |
| 状态变更 | … |

## 结果

| 项 | 证据 |
