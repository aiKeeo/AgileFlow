## 7. 少样本示例（好 vs 坏）

**坏**：只写「建模跳过（简单）」两行。  
**好**：写正式判定块，含覆盖依据与四项自检。

**坏**：`entities/User.md` 只有字段名。  
**好**：字段表含类型、约束、业务规则（如 `phone` 唯一、`status` 状态机）。

**坏**：把 API 字段写进 model。  
**好**：model 只写业务实体；API 留给 sol contracts。

---
