# REQ 阶段 UI 设计描述模板

> **定位**：REQ 阶段描述**界面做什么、怎么交互**——供后续方案 `contracts/UI-xxx` 与 FE 开发生成 UI。  
> **不定义视觉样式**（配色、字体、圆角、动效细节等）——**样式由用户后续决定**（方案或开发前 AskQuestion / humanTodo）。  
> AC 规范：[../phases/05-testing.md](../phases/05-testing.md)

---

## 何时新建 UID 文档

| 条件 | 动作 |
|------|------|
| 有 Web / App / 小程序等**用户可见界面** | 评估是否需 UID |
| 页面/流程 ≥1 且交互非 trivial | **新建** `atlas/requirements/ui/UID-xxx-名称.md` |
| 纯 API / CLI / 后台批处理、无 UI | **跳过**，REQ 注明「无 UI」 |
| 用户说「样式我自己定 / 先不定样式」 | 仍写 UID（结构与交互）；**样式节只写「待定」** |

**一页面/一主流程 ≈ 一份 UID**；复杂模块可拆多份，REQ 头部链全部 UID。

---

## 目录与命名

```
atlas/requirements/
├── REQ-001-{功能}.md
├── ui/
│   ├── README.md
│   ├── UID-001-{页面}.md
│   └── UID-002-{页面}.md
└── README.md
```

| 前缀 | 含义 | 阶段 |
|------|------|------|
| **UID-xxx** | UI **设计描述**（结构、交互） | 阶段 1 REQ |
| **UI-xxx** | UI **技术契约**（路由、组件、API 绑定） | 阶段 3 sol contracts |

**禁止**在 REQ 阶段写 `solution/contracts/UI-xxx`——那是方案阶段产物。

---

## UID 文档模板（`UID-001-{页面}.md`）

```markdown
# [UID-001] {页面名} — 界面描述

- 关联 REQ：[REQ-001](../REQ-001-{功能}.md)
- 平台：{与 REQ 一致}
- 状态：草稿 | 已确认
- **样式状态**：⬜ 待定 | ✅ 已有原型图
- **原型图**：`atlas/requirements/ui/prototypes/UID-001.png`  # 有则填；无则删
- **路由**：`/{path}`  # 有原型时填
- **截图选择器**：`{selector}`  # 可选；默认 body

> **用途**：描述页面结构与交互，供生成 UI 实现参考。**不含**最终视觉样式（除非已附原型图）。  
> **有强制原型**（UID「原型图」或 `atlas/tests/fe-pixel/pages.json`）→ FE 勾③须 [fe-pixel-compare](../tools/fe-pixel-compare.md) PASS。散图未声明不阻塞闸门。

---

## 1. 页面目标

- 用户在此完成：……
- 成功后跳转：……
- 失败时：……

---

## 2. 布局结构（信息架构，非视觉样式）

> **必须同时有：区域表 + ASCII 线条图（线框）。** 纯文字/纯表格 = 不合格。  
> 用区块描述信息架构，**不写**具体颜色、字号、间距数值。

### 2.1 区域表

| 区域 | 内容 | 说明 |
|------|------|------|
| 顶栏 | Logo、标题 | 可选 |
| 主区 | {主交互区} | 见「关键组件与交互」 |
| 底栏 | 协议链接 | 可选 |

### 2.2 布局线条图（强制 · ASCII 线框）

> **标题须可 grep**：`### 2.2` 或字面量 `布局线条图`。  
> 用 `┌─┐│└┘├┤` 画出上→下、左→右的区块与控件占位；**至少覆盖主路径可见控件**。  
> 禁止：只有「顶栏/主区/底栏」三个词；禁止用散文代替线框；禁止空代码块。

```
┌─────────────────────────┐
│       {顶栏}            │
├─────────────────────────┤
│   [ {控件 A}          ] │
│   [ {控件 B}          ] │
│   [ {主操作}          ] │
└─────────────────────────┘
```

**复杂页**：线框须体现分区；可拆默认态 / 关键态。

---

## 3. 关键组件与交互

| 组件 | 类型 | 交互行为 | 校验/限制 |
|------|------|----------|-----------|
| {控件} | {类型} | {行为} | {约束} |

---

## 4. 状态与反馈

| 状态 | 用户看到什么 | 可执行操作 |
|------|--------------|------------|
| 初始 | {…} | {…} |
| 加载中 | {…} | {…} |
| 成功 | {…} | — |
| 失败 | {文案见 REQ AC} | 重试 |

---

## 5. 关联 REQ 验收标准

| AC ID | 本页相关 Then | 备注 |
|-------|---------------|------|
| AC-001-01 | {Then} | |
| AC-001-02 | {Then} | |

---

## 6. 样式说明（REQ 阶段不定稿）

| 项 | REQ 阶段 | 谁决定 | 何时定稿 |
|----|----------|--------|----------|
| 配色 / 主题 | **待定** | **用户** | 方案 AskQuestion 或 humanTodo |
| 字体 / 字号 | **待定** | **用户** | 同上 |
| 组件库 / 设计系统 | **待定** | **用户** | sol: 技术栈卡片 |
| 参考稿 / Figma | 无则留空 | **用户** | humanTodo 提供链接 |

> 若用户已有参考：在 humanTodo 追加「提供 UID-001 视觉参考稿」；**仍不在 REQ 擅自定样式**。  
> 用户给出可导出图 → 拷到 `prototypes/` **且** UID 填「原型图」（仅放目录不声明 = 不阻塞闸门、不强制比）。

---

## 版本历史

| 版本 | 日期 | 变更说明 |
|------|------|----------|
| 1.0 | {{日期}} | 初稿 |
```

---

## REQ 文档中引用 UID（`req.md`「界面描述」节）

在 REQ 正文增加：

```markdown
## 界面描述（可选）

| UID | 页面/流程 | 文档 | 样式状态 |
|-----|-----------|------|----------|
| UID-001 | {页面} | [UID-001-{名}.md](ui/UID-001-{名}.md) | ⬜ 待定 |
| — | 无 UI | — | — |

> 结构与交互见 UID；**视觉样式由用户后续确认**，不在 REQ 定稿。
```

---

## ui/README.md 索引模板

```markdown
# UI 设计描述索引（REQ 阶段）

> **UID = 界面结构与交互**；**UI-xxx 技术契约**在阶段 3 `solution/contracts/` 生成。

| UID | 名称 | 关联 REQ | 平台 | 样式状态 | 文档 |
|-----|------|----------|------|----------|------|
| UID-001 | {页面} | REQ-001 | {平台} | ⬜ 待定 | [UID-001-{名}.md](UID-001-{名}.md) |
```

---

## 阶段衔接

| 阶段 | 如何使用 UID |
|------|--------------|
| **1 REQ** | 新建/维护 UID；**§2 必须含区域表 + 布局线条图**（线框唯一权威）；样式标「待定」 |
| **3 sol** | 读 UID → 生成 `contracts/UI-xxx`：**只链 UID**，写路由/组件树；**链 API 时须 §字段绑定**；**禁止粘贴整图** |
| **4 dev FE** | Read `UID-xxx` + **UI-xxx §字段绑定**；dev 只链 UI，**禁止** dev 内映射表；② 按步骤写码 |

---

## UID 落盘自检（勾确认前 · 可机械检查）

| # | 检查 | 不过 → |
|---|------|--------|
| U1 | 存在 `## 2.` 布局结构 | 补节 |
| U2 | 有区域表（`\| 区域 \|`） | 补表 |
| U3 | 有 **`### 2.2`** 或字面量 **`布局线条图`** | 补标题 |
| U4 | 线条图代码块内含框线字符（至少一个 `┌` 或 `+--` 或 `│`）且行数 ≥5 | 重画，禁止空块/三行糊弄 |
| U5 | 线框中的主控件能在「关键组件与交互」表找到对应行 | 对齐补全 |

**确认 REQ/UID 前**：有 UI 的每个 UID 须 U1–U5 全过；不过 → 禁止标「已确认」。

---

## 反模式

| ❌ | ✅ |
|----|-----|
| §2 只有区域表、无线框（旧「可选」写法） | **区域表 + ASCII 布局线条图** 缺一不可 |
| 线框只有「顶栏/内容/底栏」三个字 | 画出输入框、按钮、列表行等占位 |
| REQ 里写 `#FF5733`、具体 px 间距 | 写「主按钮、表单区」，样式待定 |
| REQ 直接写 `contracts/UI-001` | 写 `requirements/ui/UID-001` |
| 有页面却不建 UID | 评估后建 UID 或 REQ 注明「无 UI」 |
| Agent 替用户定配色/字体 | 标待定 + humanTodo 或 sol AskQuestion |
| FE/UI 契约再贴一份 UID 整图 | `### 布局` 链 UID；仅差量可画短 ASCII |
| FE 字段映射写在 dev | 写在 **contracts/UI §字段绑定**；dev 只链 |
